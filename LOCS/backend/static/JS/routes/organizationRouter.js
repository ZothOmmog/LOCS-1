const express = require("express");
const eventController = require("../controllers/organizerController.js");
const eventRouter = express.Router();

//eventRouter.use("/create$", eventController.create);