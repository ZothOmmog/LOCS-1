import React from 'react';
import { ProfileVisitorInfoTemplate } from '~/templates/profile-visitor-info-template';

export const ProfileVisitorSubscribesPage = () => {
    return (
        <ProfileVisitorInfoTemplate>
            <div>Список подписок</div>
        </ProfileVisitorInfoTemplate>
    );
}